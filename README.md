# Node.js Site

Простой одностраничный сайт на Node.js.

## Запуск

```bash
npm start
```

Откройте `http://localhost:3000`.

## Docker

```bash
docker build -t node-site .
docker run --rm -p 3000:3000 node-site
```
